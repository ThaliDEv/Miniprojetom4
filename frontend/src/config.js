// config.js - configurações gerais do projeto

const CONFIG = {
  apiBaseUrl: "https://api.seuprojeto.com",  // exemplo, substitua pela URL real
  themeColor: "#004aad",
  sessionTimeoutMinutes: 30,
  language: "en-US"
};

export default CONFIG;
